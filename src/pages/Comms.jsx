// Comms page is routed through CommsLayout which handles all UI orchestration.
// This is a thin routing page that lets CommsLayout manage conversation state.
export default function CommsPage() {
  return null;
}